<!DOCTYPE html>
<html lang="en">
<head>
  <% include partials/_header %>
  <style>
    img.del{
      width: 20px;
    }
  </style>
</head>
<body>
  <table>
    <thead>
      <tr>
        <th>Alias</th>
        <th>Full Link</th>
        <th>Edit</th>
        <th>Delete</th>
      </tr>
    </thead>
    <% for (each in urls){ %>
      <% if (urls[each].userID === user){ %>
      <tr>  
        <td><%= each %></td>
        <td><%= urls[each].url %></td>
        <td><a href="/urls/<%= each %>">Edit</a></td>
        <td>
          <form method="POST" action="/urls/<%= each %>/delete">
            <button><img class="del" src="http://cdn.osxdaily.com/wp-content/uploads/2010/07/512-TrashIcon-macosx1.png">Delete</button>
          </form>
        </td>
      </tr>
      <% } %>
    <% } %>
  </table>
<a href = "/urls/new">Create new tinyLink</a>
</body>
</html>